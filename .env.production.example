# ==========================================
# Variables d'environnement - Production
# ==========================================
# Copier ce fichier et remplir les valeurs
# Ces variables sont gerees par Secret Manager en production Cloud Run

# === Cloud SQL (PostgreSQL) ===
DATABASE_URL=postgresql://droitguineen:PASSWORD@localhost/droitguineen?host=/cloudsql/droitguineen:europe-west1:droitguineen-db

# === JWT Authentication ===
# Generer avec: openssl rand -base64 32
JWT_SECRET=

# === Meilisearch ===
SEARCH_URL=https://droitguineen-meili-XXXXX.a.run.app
# Generer avec: openssl rand -base64 32
MEILI_MASTER_KEY=

# === Google OAuth ===
# Depuis Google Cloud Console > APIs & Services > Credentials
GOOGLE_CLIENT_ID=
# Meme valeur, utilisee cote frontend
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# === CORS ===
CORS_ORIGIN=https://droitguineen.gn

# === Google Cloud Vision (OCR) ===
GOOGLE_CLOUD_VISION_ENABLED=true
GOOGLE_CLOUD_PROJECT_ID=droitguineen

# === Frontend (build arg) ===
NEXT_PUBLIC_API_URL=https://api.droitguineen.gn
