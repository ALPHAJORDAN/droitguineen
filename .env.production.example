# ==========================================
# Variables d'environnement - Production
# ==========================================
# Copier ce fichier en .env et remplir les valeurs:
#   cp .env.production.example .env
#
# Puis lancer:
#   docker-compose -f docker-compose.prod.yml up -d
#
# NOTE: Pour Cloud Run, ces variables sont gerees par Secret Manager (voir deploy.sh)

# === Base de donnees (PostgreSQL) ===
# Pour Docker Compose (VPS): les variables DB_USER/DB_PASSWORD/DB_NAME sont utilisees
# Pour Cloud Run: utiliser DATABASE_URL directement
DB_USER=droitguineen
DB_PASSWORD=CHANGEZ_MOI_MOT_DE_PASSE_FORT
DB_NAME=droitdatabase

# === JWT Authentication ===
# Generer avec: openssl rand -base64 32
JWT_SECRET=CHANGEZ_MOI_GENERER_AVEC_OPENSSL

# === URLs (OBLIGATOIRE - adaptez a votre domaine) ===
# URL publique du backend (celle que le navigateur utilise)
# Exemples: https://api.droitguineen.gn, http://VOTRE_IP_VPS:4000
NEXT_PUBLIC_API_URL=http://VOTRE_IP_VPS:4000

# CORS: URL du frontend (doit correspondre exactement)
# Exemples: https://droitguineen.gn, http://VOTRE_IP_VPS:3000
CORS_ORIGIN=http://VOTRE_IP_VPS:3000

# URL frontend pour les liens dans les emails
FRONTEND_URL=http://VOTRE_IP_VPS:3000

# === Meilisearch ===
# Generer avec: openssl rand -base64 32
MEILI_MASTER_KEY=CHANGEZ_MOI_GENERER_AVEC_OPENSSL

# === Google OAuth (optionnel) ===
# Depuis Google Cloud Console > APIs & Services > Credentials
GOOGLE_CLIENT_ID=
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# === Google Cloud Vision OCR (optionnel) ===
GOOGLE_CLOUD_VISION_ENABLED=false
GOOGLE_CLOUD_PROJECT_ID=

# === Email SMTP (optionnel) ===
# Creer un App Password: Google Account > Security > 2-Step Verification > App passwords
SMTP_USER=
SMTP_APP_PASSWORD=

# === Ports (optionnel, valeurs par defaut) ===
# FRONTEND_PORT=3000
# BACKEND_PORT=4000
